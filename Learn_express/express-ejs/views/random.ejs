<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Document</title>
    </head>
    <body>
        <h1>Random Number: <%= num %></h1>
        <% if (num % 2 === 0) { %>
        <h3>Angka Genap</h3>
        <% } else { %>
        <h3>Angka Ganjil</h3>
        <% } %>

        <h3>Angka <%= num %> adalah <%= num % 2 === 0 ? 'genap' : 'ganjil' %></h3>
    </body>
</html>
